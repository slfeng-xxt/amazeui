import { IHeadingNode, IPageNode } from "documentalist/dist/client";
import * as React from "react";
export interface INavigatorProps {
    items: Array<IPageNode | IHeadingNode>;
    onNavigate: (id: string) => void;
}
export interface INavigatorState {
    query: string;
    selectedIndex: number;
}
export declare class Navigator extends React.PureComponent<INavigatorProps, INavigatorState> {
    state: INavigatorState;
    private inputRef;
    private resetState;
    private sections;
    private handleQueryChange;
    private handleKeyDown;
    render(): JSX.Element;
    renderHotkeys(): JSX.Element;
    componentDidMount(): void;
    private getMatches();
    private renderPopover();
    private handleFocusSearch;
    private handleSetSearchInputRef;
    private handlePopoverInteraction;
    private handleResultHover;
    private selectNext(direction?);
}
